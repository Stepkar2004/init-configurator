# Now

> Updated at every chunk boundary (gates pass → this file + log.md → commit).
> Cold start reads: `../vision.md` → this file → `roadmap.md`.

**As of 2026-07-11 (published; socials skill authored):**

The genome survived its first real generation. traffic-rl (the first child) ran phase 0
on it, evolved it — nested-skill consolidation, a `workflow` skill, authoring standards,
never-push and posts conventions — and those genes were absorbed back here as reviewed
diffs. The genome now ships inside the package: `initc spawn <dir>` copies it into any
project, additive-only (roadmap step 3's spawn half, promoted after one proven by-hand
run; ADR 0002). `spawn` is additive by default; `--force` refreshes a child's skills
(added 2026-07-11). Skills here: `project-base`, `workflow`, `skill-manager`,
`bootstrap`, `socials` — the transferable four mirrored byte-identical in
`src/init_configurator/genome/` (test-enforced).

- Published at github.com/Stepkar2004/init-configurator (2026-07-11); repo description +
  14 topics applied for discoverability (the empty-topics gap `socials/references/github.md`
  warned about).
- `socials` skill authored (2026-07-11, raw) from a four-source research pass. The launch
  post was reworked through it and is ready to post (`docs/posts/2026-07-11-the-amputation.md`,
  local); posting is the owner's to do.
- `initc spawn --force` shipped: updates existing skills only, docs/standards untouched,
  never deletes. README restructured to the reader's path (Quickstart → skills → tools →
  manifest).
- `docs/vision.md` still unfilled — waiting on the owner; the amputation review still
  owed is now the pre-publish review of this whole state.
- `initc absorb` as a command: still by hand, same promotion bar spawn had.

**Next action:** owner posts the launch reflection; then rot-check pass #1 (roadmap
step 4), and keep absorbing whatever traffic-rl's implementation phases teach.
